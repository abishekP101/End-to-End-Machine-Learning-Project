from setuptools import setup, find_packages

setup(
    name="us_visa",
    version="0.1",
    packages=find_packages(),  # This will find all your packages like us_visa.components, etc.
    install_requires=[],       # You can put package dependencies here or in requirements.txt
)
